import forms from '@tailwindcss/forms';

export default {
	plugins: [
		forms,
	],
};

const config = {
  content: [
       // more lines
        "./node_modules/flowbite-svelte-icons/**/*.{html,js,svelte,ts}",
    ],
    // more lines
}  ;